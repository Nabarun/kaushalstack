{
  "mcpServers": {
    "kaushalstack": {
      "command": "npx",
      "args": ["-y", "kaushalstack-mcp"],
      "env": {
        "KAUSHALSTACK_API_URL": "${KAUSHALSTACK_API_URL}",
        "KAUSHALSTACK_API_TOKEN": "${KAUSHALSTACK_API_TOKEN}"
      }
    }
  }
}

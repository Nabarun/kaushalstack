mcp>=1.2.0
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27            # only needed for live (non-mock) gateway calls
cryptography>=43.0     # Ed25519 signing + X25519/AES challenge (PyNaCl/pycryptodome no longer needed)
anthropic>=0.50        # Claude API — used by agent.py
